<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.scanelectronicsystems">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ScanElectronicSystems">
        <activity android:name=".SolutionsActivity"></activity>
        <activity android:name=".ReviewFinalActivity" />
        <activity android:name=".OtherComplaints" />
        <activity android:name=".ComplaintsActivity" />
        <activity android:name=".ComplaintTypeActivity" />
        <activity android:name=".ProductDetailsActivity" />
        <activity android:name=".ProductSub2MenuActivity" />
        <activity android:name=".ProductSub1MenuActivity" />
        <activity android:name=".AboutUsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>